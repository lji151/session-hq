# Ideas inbox

One line per idea, appended, newest at the bottom. Format:

```
- YYYY-MM-DD | the idea in one line | domain
```

An idea in here is **not** a commitment and **not** on anyone's plan. When one graduates into
real work, move it into the relevant `status-<domain>.md` and leave the inbox line where it is.

Started {{DATE}}.

